<% recording_studio_page_nav(
     title: @ticket.subject,
     page_nav_back_url: help_messages_path_for_desk,
     page_nav_anchor_url: help_messages_path_for_desk,
     page_nav_anchor_label: "Close"
   ) %>

<div class="flex w-full flex-col gap-4">
  <%= render FlatPack::PageTitle::Component.new(
    title: @ticket.subject,
    subtitle: support_ticket_status_label(@ticket),
    variant: :h1
  ) %>

  <%= render "recording_studio_messages/message_groups/desk",
             group_recordings: @group_recordings,
             group_recording: @group_recording,
             message_recordings: @message_recordings,
             current_actor: current_support_actor,
             form_url: support_messages_form_url(@group_recording),
             return_to: staff_desk_return_to,
             desk_height_class: "h-[calc(100dvh-12rem)]" %>
</div>
