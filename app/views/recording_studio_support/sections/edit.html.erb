<% recording_studio_page_nav(
     title: "Edit section",
     page_nav_back_url: section_path(@section_recording),
     page_nav_anchor_url: root_path,
     page_nav_anchor_label: "Close"
   ) %>

<div class="flex w-full flex-col gap-6">
  <%= render FlatPack::PageTitle::Component.new(
    title: "Edit section",
    variant: :h1
  ) %>

  <%= render "form",
             url: section_path(@section_recording),
             method: :patch,
             cancel_url: RecordingStudioSupport::Admin::Queries.admin_sections_screen_path %>
</div>
